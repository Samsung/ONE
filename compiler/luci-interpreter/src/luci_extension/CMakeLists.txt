file(GLOB_RECURSE SOURCES "Nodes/*.cpp")
file(GLOB_RECURSE HEADERS "${LUCI_INTERPRETER_INCLUDE_DIR}/luci/*.h")
list(APPEND SOURCES ${HEADERS})

message(STATUS ${SOURCES})

add_library(${LUCI_INTERPRETER_LUCI_EXTENSION} STATIC ${SOURCES})
set_target_properties(${LUCI_INTERPRETER_LUCI_EXTENSION} PROPERTIES POSITION_INDEPENDENT_CODE ON)
target_include_directories(${LUCI_INTERPRETER_LUCI_EXTENSION} PUBLIC "${LUCI_INTERPRETER_INCLUDE_DIR}")
target_link_libraries(${LUCI_INTERPRETER_LUCI_EXTENSION} PUBLIC luci_lang)
target_link_libraries(${LUCI_INTERPRETER_LUCI_EXTENSION} PRIVATE nncc_common)
